# Custom API URL override. If left blank, the app dynamically falls back to:
# - http://localhost:5000 (when running on localhost)
# - https://backend-rockefeller-finance.onrender.com (when running in production)
# REACT_APP_API_URL=
